generated: '2026-08-13'
method: generated
source: >-
  openapi/ (operationIds verified against the corrected specs) +
  https://app.zenserp.com/documentation "Common Use Cases" section
name: Zenserp Agent Skills
description: >-
  Packaged agent skills for the marquee Zenserp flows, mirroring the use cases
  Zenserp documents itself. Every operationId referenced below exists in this
  repo's OpenAPI documents and was confirmed against a live probe of the API host
  on 2026-08-13. Zenserp publishes no AGENTS.md, no skills and no llms.txt for its
  API documentation host, so these are generated by API Evangelist.
provider_published: false
skill_count: 3

skills:
  - name: zenserp-rank-tracking
    file: zenserp-rank-tracking.md
    api: zenserp:zenserp-search-api
    description: Track where a domain ranks for a keyword in a specific country, language and city.
    operations: [listLocations, listCountries, listLanguages, search, getStatus]
  - name: zenserp-bulk-serp-batch
    file: zenserp-bulk-serp-batch.md
    api: zenserp:zenserp-batch-api
    description: Fetch thousands of SERPs asynchronously and receive the results on a webhook.
    operations: [submitBatch, listBatches, getBatch, getStatus]
  - name: zenserp-ad-verification
    file: zenserp-ad-verification.md
    api: zenserp:zenserp-search-api
    description: Verify whether a Google Ad is actually being served for a keyword in a specific city.
    operations: [listLocations, search, getStatus]

shared_rules:
  - Authenticate with the `apikey` HEADER, not the query parameter.
  - 'The API spans two base URLs: /api/v2 for search, status and the list endpoints; /api/v1 for batches, trends and shopping product pages.'
  - 403 is ambiguous (bad key OR exhausted quota OR unentitled plan) -- disambiguate with GET /api/v2/status.
  - 404 means "no results found", not "no such endpoint".
  - No rate-limit response headers exist; quota is readable only from GET /api/v2/status.
  - No idempotency key -- a retried POST /api/v1/batches can create a duplicate batch.
  - Offset pagination via `start`/`num`, default 10, hard ceiling 300 results.
